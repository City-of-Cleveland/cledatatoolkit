[build-system]
requires = ["setuptools>=61.0", "geopandas"]
build-backend = "setuptools.build_meta"

[project]
name = "cle-data-toolkit"
version = "0.0.1"
readme = "README.md"
requires-python = ">=3.7"
